// Todo 先把简单的做了再回过头来解决了这道大综合 
#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cctype>
#include <algorithm>
using namespace std;
const int MAXN = 
const int MAXLEN = 
class TreeArray {
private:
    int ar[MAXLEN];
    inline int lowbit(int a) {
        return a&(-a);
    }
    int len;
public:
    int insert(int w, ) {
        for ( ; a <= len; a += lowbit(a))
    }

int main() {
    
}
